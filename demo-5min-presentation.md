# 5 分钟个人陈述演示稿（脱敏）

> 使用方式：进入 Presentation 后，建议按 Segment 1–6 依次提交。全文约 5 分钟，姓名、学校、实验室和作者身份均已脱敏。

## Segment 1｜研究动机（约 45 秒）

各位老师好，我今天汇报的项目是“用最优控制方法筛选语言模型预训练数据”。我对这个问题感兴趣，是因为语言模型训练正在遇到一个很现实的限制：网页语料规模增长得很快，但 GPU 时间、显存和能源预算增长得没有那么快。很多工作把重点放在设计更大的模型，或者收集更多数据，但在固定计算预算下，下一批数据应该选什么，同样会决定模型最后学到什么。

最直接的做法是随机抽样，或者按照规则过滤低质量网页。另一类方法使用当前模型的 perplexity，优先选择模型容易预测的文本。问题是，模型当前容易预测，不一定意味着这条数据对未来参数更新最有价值。我的研究问题是：能不能把训练过程看成一个动态系统，根据模型现在的状态，估计一条数据对未来训练轨迹的影响，再决定下一批数据？

## Segment 2｜项目与个人贡献（约 50 秒）

这是一个三人小组的复现与扩展项目。我个人主要负责四部分工作。第一，我把预训练过程抽象成离散时间动态系统，把模型参数看作状态，把每个 batch 的数据选择看作控制变量。第二，我实现了一个近似的 costate 更新，用它估计某个样本对未来验证损失的影响。第三，我负责数据管线，包括网页去重、语言识别、长度分桶、来源分层和 batch 采样。第四，我设计并运行了消融实验，检查结果到底来自最优控制建模，还是仅仅来自更激进的数据过滤。

这里我想特别说明个人边界：问题定义和理论推导是组内共同完成的，我没有独立证明最优性定理；我能负责解释的是选择器训练循环、采样器实现、日志分析和实验结果。我们把所有训练配置、随机种子和验证集固定下来，避免把工程差异误认为算法提升。

## Segment 3｜方法设计（约 55 秒）

整个系统分成三个步骤。首先，原始网页语料经过 URL 去重、近似文本去重、语言识别和长度过滤，得到候选池。我们把候选池按教育、科技、新闻和论坛四个来源分层，避免某一个来源因为数量大而完全主导采样。其次，对每一个候选 batch，我们计算当前模型的梯度统计和近似 costate，得到一个数据选择分数。这个分数不只表示“模型现在能不能预测”，还试图表示“这个样本对未来训练状态有没有影响”。最后，采样器同时满足分数排序和来源配额，把选中的数据送入训练，并把 loss、梯度、来源和采样分数写入日志。

与 perplexity 相比，这个方法的核心差别是加入了训练轨迹信息。与单纯的 gradient-norm 相比，它希望考虑未来目标，而不是只看当前梯度大小。当然，这也带来了额外成本：每轮选择都需要计算统计量，costate 还是一个近似量，不能把它说成精确的长期价值。

## Segment 4｜实验设置与结果（约 60 秒）

实验使用 1.2 亿 token 的脱敏网页语料，基础模型是 125M 参数的 decoder-only Transformer，训练 20,000 steps。我们比较四个方法：Uniform 随机抽样、Perplexity、Gradient-norm，以及我们实现的 PDS。所有方法使用相同模型、相同训练步数、相同验证集和相同的总 token 预算。

Uniform 的验证集 loss 是 3.18，下游分类准确率是 72.4%，达到 loss 2.95 需要约 100M token。Perplexity 的 loss 是 3.11，需要 92M token。Gradient-norm 的 loss 是 3.08，需要 87M token。PDS 的 loss 是 3.02，需要 76M token，下游准确率是 74.6%。在这个设置下，PDS 相对于随机抽样减少了约 24% 的 token 消耗。

但我不想把这个结果说得过度乐观。去掉 costate、只保留当前梯度范数后，loss 变成 3.05，说明一部分收益确实来自更强的梯度信号，而不一定完全来自动态系统建模。另外，如果取消来源分层，论坛数据会被过度选择，数学题下游准确率下降 2.1 个百分点。因此“训练 loss 更低”不能直接等价为“所有任务都更好”。

## Segment 5｜失败分析与边界（约 50 秒）

项目早期我犯过一个概念错误：把选择分数高直接当成数据质量高。结果选择器偏向短文本、格式规整且容易被当前模型预测的网页。它们确实能让训练 loss 下降，但对需要长链路推理的任务帮助有限。我们后来增加长度分桶、来源配额和固定的跨领域验证集，才发现训练效率和泛化能力之间存在明显差异。

这个项目还有几个边界。第一，costate 近似依赖训练轨迹，换模型大小或优化器后需要重新校准。第二，选择器本身有额外的前向和反向计算，如果只报告节省的 token，可能掩盖真实 FLOPs 并没有下降。第三，我们只验证了 125M 和 350M 两个规模，不能凭这组实验直接声称对 400B 模型成立。第四，网页数据存在语言、主题和来源偏差，选择策略可能放大原始语料中的不平衡。

## Segment 6｜研究计划与收束（约 45 秒）

下一步我想把“选择收益”和“选择成本”放进同一个目标函数，比较总训练 FLOPs，而不是只比较 token 数。我也想测试分布外任务，例如数学推理、代码生成和长文档问答，看选择器是否在训练集指标之外保持稳定。另一个方向是把不确定性引入采样策略：当选择器对两个样本判断接近时，不应该强行相信一个分数，而应该保留探索比例。

这项工作让我认识到，一个看似有效的方法，必须同时回答三个问题：它解决了什么明确问题，为什么比简单 baseline 更必要，以及结果在哪些条件下不成立。我的长期兴趣是有限计算预算下的可靠决策，希望继续学习强化学习、在线学习和语言模型 agent，把数据选择、工具调用和停止策略放在统一的决策框架里。我的汇报到这里，谢谢老师。

