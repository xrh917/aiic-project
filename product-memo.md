# AIIC Product Memo

> 面向理工科本科生保研复试的 AI Professor Interview Simulator
>
> 公网地址：`https://82.156.250.184/`  
> GitHub：`git@github.com:xrh917/aiic-project.git`

## 1. 目标用户与核心痛点

### 目标用户

- 理工科本科生
- 正在准备保研复试
- 有科研项目或课程项目，但缺少目标导师视角的练习

### 核心场景

> 学生准备向目标导师介绍科研项目，希望练习在有限时间内讲清楚问题、方法、个人贡献和实验边界，并应对教授临时打断。

### 痛点证据

- 普通 AI 面试官的问题过于官方，和目标导师研究方向关联弱
- AI 容易被候选人的最后一句话带偏，缺少固定考察 agenda
- 学生很少练习被打断后的恢复，容易重复背景、超时或遗漏核心贡献
- 反馈常常只有一个总分，缺少原话证据和可执行建议

## 2. 产品设计说明

### 产品判断

> 产品核心不是让 AI 多问几个问题，而是模拟一个有研究兴趣、有考察 agenda、会主动打断并控制节奏的目标教授。

### 核心闭环

`Setup → Professor Profile → Presentation → Content-driven Interruption → Evidence-based Report → Retry`

### 关键功能

1. 根据教授资料和候选人材料生成教授画像与 hidden agenda。
2. 在个人陈述中，根据研究兴趣、证据缺口、方法取舍和个人贡献判断是否打断。
3. 记录实际陈述用时、打断次数、打断回答耗时和恢复表现。
4. 用多个能力维度生成带原话证据的反馈，而不是只给一个总分。
5. 支持静默观察、标准追问和高压追问，并可对比不同模式下的表现。

### 音频与输入设计

音频不是独立的“炫技功能”，而是为了让练习更接近真实复试。产品提供三条可互相切换的路径：

- **文字演示模式**：系统提供一份完整的示例个人陈述，用户点击“填入示例”即可跑通演示闭环。
- **AI 朗读模式**：当用户提交文字时，教授问题和候选人示例回答可以用浏览器 `speechSynthesis` 朗读，用户无需配置复杂 TTS 服务。
- **真人语音模式**：浏览器获取麦克风，使用 Speech Recognition 将语音转为文本；文本仍由同一个打断 Controller 和评分流程处理。

语音模式必须是增强能力，而不是单点依赖。浏览器不支持语音、用户拒绝权限、网络识别中断或 ASR 服务失败时，界面保留文字输入并继续完成整个面试流程。

稳定性方案包括：

1. 麦克风启动前进行权限和安全环境检查，只允许一次启动请求；
2. 对浏览器自动结束识别进行受控重连，避免重复启动和状态抖动；
3. 以 10–20 秒为一个语义段提交，而不是依赖 token 级实时流；
4. 保留识别缓冲区，识别重连时不丢失已经得到的文本；
5. 打断回复阶段复用同一套语音输入组件，但不再次触发打断判断；
6. 记录 ASR 错误并提供明确状态，任何音频故障都不阻塞文字路径。

后续版本可以把浏览器 Speech Recognition 替换为服务端 ASR（例如 Whisper-compatible 接口），但输入输出契约保持不变：`audio segment → transcript → interview controller`。

### 刻意不做的功能

- Webcam、表情识别、眼神检测：与核心训练目标无关，且会增加隐私和实现成本。
- 数字人和复杂 TTS：先保证内容驱动的打断闭环。
- 自动爬取教授论文：要求用户提供教授资料，避免事实错误和范围膨胀。
- 登录、排行榜、数据库：MVP 只保存当前浏览器 session。
- 强制深度问答：陈述评估和正式问答可选，避免流程过长。

## 3. 版本迭代记录

| 阶段 | 发现的问题 | 修改与原因 |
| --- | --- | --- |
| 初始方案 | 普通聊天式 AI 面试容易跑题 | 引入教授画像和 hidden agenda |
| 早期打断 | 固定或关键词打断不够像教授 | 改为 DeepSeek 结合上下文判断 |
| 时间设计 | 页面倒计时会让用户只关注数字 | 记录实际用时，同时保留可见剩余时间提醒 |
| 反馈设计 | 单一分数无法解释问题 | 改为能力维度、原话证据和改进建议 |
| 面试流程 | 深度问答固定出现，演示过长 | 改为 Setup 中可选的面试环节 |
| 输入方式 | 语音权限和识别存在环境差异 | 文字输入保证完整闭环，语音作为增强 |
| 音频体验 | 用户希望有材料可直接照着练习，纯文字演示不够自然 | 增加示例稿、AI 朗读和真人语音三条互补路径 |
| 语音稳定性 | 识别会自动结束、开头漏字或重复启动 | 增加启动锁、受控重连、语音缓冲和明确降级提示 |

## 4. 下一步设计

如果再给一周，按优先级说明：

1. 用真实学生和导师反馈校准六维评分 rubric。
2. 将浏览器 Speech Recognition 抽象为可替换的 ASR 服务，接入 Whisper-compatible API，并比较识别准确率、延迟和成本。
3. 增加语音端点检测、回声/噪声处理和更稳定的 10–20 秒语义分段，减少开头漏字与断线。
4. 为教授问题和示例回答增加可选 AI 朗读，允许用户调节语速并随时切回文字。
5. 支持跨轮报告比较，展示同一弱点是否改善。
6. 评估 AI 反馈与真实导师评价的一致性。
7. 增加更多专业方向的教授风格模板，同时保持 agenda 可控。
8. **项目逻辑链拆解**：将简历中的每个项目拆成“问题定义 → 关键假设 → 方法选择 → 对照方案 → 实验结果 → 局限与下一步”，让教授围绕同一项目做横向比较（为什么不用另一种方法）和纵向追问（每一步如何影响下一步），而不是只沿着模型生成的叙事继续提问。
9. **追问资料包与长期上下文**：为每个项目建立可检索的 evidence pack，保存数据来源、实验设置、基线、个人负责边界和结果解释。连续追问时优先从资料包取证，发现材料不足时明确标记“需要补充材料”，避免模型在追问过程中逐渐泛化或编造。
10. **表达逻辑审计**：增加独立的结构分析器，检查因果跳跃、结论先于证据、概念层级混用、重复铺垫和缺少转折等问题。报告中用“原句 → 逻辑问题 → 对听众的影响 → 重排建议”呈现，并允许将重排后的段落直接生成下一轮练习稿。

## 5. AI 工具使用

| 工具 | 使用环节 | 说明 |
| --- | --- | --- |
| DeepSeek API | 教授画像、打断判断、演示续讲、结构化报告 | 通过服务端代理调用，失败时使用本地兜底 |
| Codex | 前端、后端代理、测试、部署和调试 | AI 辅助实现，产品范围和交互取舍由项目完成者决定 |
| 浏览器 Speech Recognition | 语音输入实验 | 非必需能力，文字输入始终可用 |
| 浏览器 speechSynthesis | 教授问题和示例回答朗读 | 低成本音频反馈，不依赖复杂 TTS 服务，失败时显示文字 |
| AI 辅助写作 | 脱敏演示材料和口语化陈述稿 | 仅使用合成数据，不包含真人隐私 |

人工完成的关键判断包括：目标用户选择、教授 agenda、打断模式、评分维度、MVP 边界和最终测试。

## 6. 评估与成功标准

MVP 的成功标准不是“模型说得像人”，而是用户能否完成一次有时间压力的闭环练习，并得到可复盘的证据。首轮测试关注：

- 用户是否能在 1 分钟内完成 Setup 并开始陈述；
- 教授打断是否与候选人当前内容相关，而不是由单个关键词触发；
- 打断回复阶段是否清楚、可用文字或语音完成，并且计时不中断；
- 报告是否指出原话、问题类型、影响和下一步改法；
- 在同一材料下切换模式，打断次数、恢复时间和报告分数是否呈现可解释差异；
- 语音失败时，用户是否可以无感切换到文字并完成流程。

这些指标将通过真实学生试用记录和少量导师盲评逐步校准，而不是把一次模型输出当作客观录取分数。

## 7. 风险与边界

- AI 反馈是训练建议，不代表真实导师的录取结论；
- 教授信息和候选人材料默认由用户提供，演示环境使用脱敏或生成材料；
- 语音数据只在当前会话中用于转写，不建立用户画像或长期数据库；
- 网络、浏览器和 ASR 服务可能导致语音体验下降，因此文字模式始终是产品底线；
- 评分结果会固定在本轮 session 中，避免同一份记录因重复调用模型而反复变化。

## 8. 已识别但尚未解决的痛点

当前 MVP 已经能完成教授画像、定时打断、回复恢复和证据反馈，但在更长的科研项目复试中仍有三类不足：

1. **简历逻辑容易反过来牵着教授走**：模型可能被候选人材料中的表述带偏，只生成顺着原叙事的追问，较少主动要求横向比较方法或纵向拆解项目链条。
2. **连续追问需要反复补充资料**：如果没有实验设置、基线、数据来源和个人贡献等细节，模型在多轮追问后会逐渐失去事实锚点，反馈也会变得笼统。
3. **表达逻辑问题不容易被指出**：普通评分通常只判断信息是否出现，不检查因果关系和论证顺序，因此内容不变时，学生很难通过 AI 真正提升表达的逻辑性。

这三类问题已纳入下一阶段设计，不在当前 MVP 中伪装成已经解决的能力。
