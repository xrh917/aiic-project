# AIIC 详细脱敏演示材料

> 用途：演示 Setup → Professor → Presentation → Interruption → Q&A → Report 的完整流程。
> 姓名、学校、实验室和作者身份均已脱敏；实验数字是为面试演示整理的合成数据，不对应任何真实个人。

## Candidate materials

姓名：陈同学（测试数据）

本科院校：华南某综合大学

专业：计算机科学与技术，2026 届

课程基础：数据结构、概率统计、机器学习、数据库系统、操作系统、最优化方法。

### 科研项目：用最优控制做语言模型预训练数据筛选

#### 研究问题

大规模语言模型预训练通常从 Common Crawl 等网页语料中抽取数据。简单按规则过滤或按困惑度排序，会遇到两个问题：高质量数据不一定在当前模型上最容易预测，数据筛选策略还会改变后续训练轨迹。我们的目标不是只找“看起来质量高”的句子，而是判断哪些数据在当前训练阶段最值得被模型看到。

#### 我的工作

我和两位同学完成了一个小型复现项目。我个人负责数据选择器、训练日志分析和消融实验，具体包括：

1. 将预训练过程抽象为离散时间动态系统，把模型参数作为状态，把每一批训练数据的选择作为控制变量；
2. 根据 Pontryagin Maximum Principle 推导近似的 costate 更新，并实现基于梯度影响的选择分数；
3. 写数据去重、语言识别、长度过滤和 batch 采样脚本；
4. 设计固定数据量、固定模型大小和固定训练步数的对照实验；
5. 分析选择器在不同领域数据上的偏差，以及选择分数和最终验证损失之间是否一致。

#### 实验设置

我们使用 1.2 亿 token 的脱敏网页语料，按教育、科技、新闻和论坛四个来源分层。基础模型为 125M 参数的 decoder-only Transformer，训练 20,000 steps，比较 Uniform、Perplexity、Gradient-norm 和 Ours-PDS 四种方法。

| 方法 | 验证集 loss | 下游分类准确率 | 达到 loss 2.95 所需 token |
| --- | ---: | ---: | ---: |
| Uniform | 3.18 | 72.4% | 100M |
| Perplexity | 3.11 | 73.1% | 92M |
| Gradient-norm | 3.08 | 73.8% | 87M |
| Ours-PDS | 3.02 | 74.6% | 76M |

PDS 在有限数据预算下减少了约 24% 的 token 消耗。消融实验显示，去掉 costate、只保留当前 batch 的梯度范数后，loss 改善缩小到 3.05；不做来源分层时，论坛数据被过度选择，下游数学题准确率下降 2.1 个百分点。

#### 失败与局限

第一次实验中，我把“选择分数高”直接当成“数据质量高”，结果选择器偏向短文本和格式稳定的网页。后来增加长度分桶、来源分层和固定验证集，才发现训练效率提升与泛化能力提升并不完全一致。

目前仍有三个问题：costate 近似依赖训练轨迹，换模型规模后需要重新校准；选择器需要额外前向/反向计算；我们只做了 125M 和 350M 两个规模，不能直接声称对更大模型成立。

### 第二个项目：在线学习排序的探索策略

为了理解模型如何从用户反馈中学习，我还复现了一个在线 pairwise learning-to-rank 原型。系统每轮只比较不确定的文档对，而不是随机打乱整个列表。我的工作是实现置信区间、记录点击反馈，并比较 epsilon-greedy 与 uncertainty-only exploration。

在两个公开排序数据集的离线回放中，uncertainty-only 的累计 regret 低于 epsilon-greedy；但在点击偏差很强的模拟器中，它会过早相信错误的 pairwise 顺序。因此我认为探索效率和反馈偏差需要一起评估，不能只看最终 NDCG。

### 个人陈述中的研究兴趣

我希望继续研究“有限计算预算下的可靠决策”。具体来说，我想把数据选择、在线学习和语言模型 agent 的工具调用联系起来：系统不应该只追求单次任务准确率，还要知道什么时候需要更多信息、什么时候应该停止探索，以及如何用不确定性解释自己的选择。

我的不足是理论推导还不够扎实，尤其是最优控制中的连续时间假设、离散训练过程中的近似误差，以及在线学习 regret bound 之间的关系。我没有做过大规模分布式训练，只在单机多卡环境中完成过实验。

## PPT / personal statement

### Slide 1：问题与动机

预训练数据越来越多，但计算预算并没有同步增长。问题不是“有没有数据”，而是“下一批数据应该选什么”。如果数据选择只根据静态质量分数，就无法反映模型当前的学习状态。

### Slide 2：方法直觉

我们把训练看作一个动态系统，把模型参数当作状态，把数据选择当作控制变量。目标是在有限 token 预算下尽快降低验证损失。最优控制提供了一个思路：不仅看当前数据的梯度，还要估计它对未来训练轨迹的影响。

### Slide 3：系统实现

数据经过去重、语言识别、长度分桶后进入候选池。每个 batch 计算近似选择分数，采样器按照分数和来源配额抽取数据，同时把训练 loss、梯度统计和数据来源写入日志。

### Slide 4：实验与结果

在 125M 模型和 1.2 亿 token 的设置下，PDS 将达到相同验证损失所需的 token 从 100M 降到 76M。这个结果说明数据选择可以提高数据利用率，但不等于所有下游任务都会同步提升。

### Slide 5：教授可能追问

如果教授问“为什么不用 perplexity”，我的回答是：低 perplexity 只能说明当前模型容易预测，不代表样本对未来参数更新有最大价值。我们的消融中，perplexity 的 loss 是 3.11，而 PDS 是 3.02，但这个差异依赖固定的训练预算和验证集。

如果教授问“你个人到底做了什么”，我的回答是：我负责选择器实现、costate 近似的训练循环、数据来源分层和主要消融实验；论文中的问题定义和理论推导由组内同学共同完成，我没有声称独立完成理论证明。

如果教授问“这个结果能否推广到 400B 模型”，我的回答是：不能从当前实验直接推出。最多只能说小规模实验和 scaling-law 外推提供方向性证据；真正验证需要更大模型、更多 token 和严格的 compute-matched 对照。

### Slide 6：下一步

下一步我想验证选择策略在分布外任务上的稳定性，并把选择成本纳入目标函数。如果选择器每次都要额外计算梯度，理论上的 token 节省可能被选择开销抵消。我会比较“选择器开销 + 训练开销”的总 FLOPs，而不是只报告训练 token 数。

## Target professor

教授姓名：周教授（脱敏画像）

学校 / 实验室：华北某重点大学 · 决策智能与交互学习实验室

教授研究方向：机器学习、信息检索、序列决策、在线学习、语言模型 agent、可信推荐系统。

教授主页 / 个人介绍：

周教授长期研究机器学习与信息检索的交叉问题，特别关注序列决策和在线反馈场景。研究组把算法理论、用户行为建模和可部署系统放在同一个问题里讨论。教授通常会追问：问题定义是否准确、方法相对于简单 baseline 是否必要、实验是否公平、指标是否真的支持结论，以及学生本人是否理解实现细节。

面试风格：语气平静但会主动打断。候选人说“效果提升明显”时，教授会要求数字、置信区间和对照组；候选人说“我们设计了方法”时，教授会要求拆分个人贡献；候选人把相关性说成因果性时，教授会要求指出假设和失败案例。教授不喜欢无限展开背景，通常会在一个主题追问 1–2 次后切换到下一个主题。

教授可能重点关注：

1. 最优控制建模与实际训练过程之间的对应关系；
2. PDS 相对于 perplexity、gradient-norm 的必要性；
3. token 节省是否抵消选择器计算开销；
4. 小模型实验能否支持对大模型的结论；
5. 数据偏差、分布外泛化和实验公平性；
6. 学生能否区分共同工作、个人实现和理论贡献。

代表论文方向（作者身份已脱敏）：

- 《通过最优控制进行语言模型数据选择》：将数据选择表述为广义最优控制问题，并用必要条件近似选择高价值预训练数据。
- 《通过分治进行在线成对排序学习》：只在排序不确定的文档对上探索，以降低在线学习排序的 regret。
- 《面向序列决策的用户反馈建模》：研究搜索/推荐系统中的隐式反馈、探索和长期满意度。

## Hidden interview agenda（供产品测试，不向用户展示）

1. 研究问题与方法必要性：为什么数据选择需要动态系统视角？最多追问 2 次。
2. 实验设计与证据：基线、数据预算、compute matching、统计稳定性。最多追问 2 次。
3. 个人贡献与工程细节：选择器、costate、采样器、日志和失败排查。最多追问 2 次。
4. 研究潜力与边界：外推、大模型、分布外泛化、下一步研究。最多追问 2 次。

